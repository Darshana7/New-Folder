package com.te.hasarelation;

public class Rachita {
	public static void main(String[] args) {
		Battery battery=new Battery(4000, 100, "lenovo");
		Operatings operatings=new Operatings(brand, size, 64);
		HardDisk hardDisk=new HardDisk(brand, size, type);
		Processor processor=new Processor(brand, core, speed);
	}
}
